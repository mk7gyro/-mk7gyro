# コードベース参加ガイド

## 1. 現在のリポジトリ構造（2026-04-04時点）

現時点で、このリポジトリには実質的な実装ファイルが存在せず、以下のみが配置されています。

- `.git/` : Git履歴・設定
- `.gitkeep` : 空リポジトリを保持するためのプレースホルダ

## 2. 新規参加者が最初に知っておくべき重要事項

1. **まだ初期化段階のリポジトリ**
   - アプリケーションコード、テスト、CI設定、依存定義（`package.json` / `pyproject.toml` など）が未配置です。
2. **開発規約はこれから決まる可能性が高い**
   - 命名規則、ディレクトリ分割、レビュー基準、リリースフローは未確定である前提で進める必要があります。
3. **最初の設計判断が将来コストを左右する**
   - 初手で「言語」「フレームワーク」「テスト戦略」「CI」を揃えると、以後のオンボーディングが大幅に楽になります。

## 3. 次に学習・整備すべき項目（優先順）

### 優先度A（すぐ決める）

- **技術スタック**
  - 例: TypeScript + Node.js / Python + FastAPI / Go など
- **リポジトリ標準構成**
  - 例: `src/`, `tests/`, `docs/`, `scripts/`, `.github/workflows/`
- **パッケージ管理と実行方法**
  - 例: `npm` / `pnpm` / `uv` / `poetry` / `go` コマンド

### 優先度B（初期実装と同時）

- **静的解析・フォーマッタ**
  - ESLint/Prettier, Ruff/Black など
- **テスト方針**
  - 単体テストを最初に導入し、最小限のCIで自動実行
- **ブランチ・PR運用**
  - main保護、レビュー最低人数、コミット規約

### 優先度C（早めに準備）

- **運用ドキュメント**
  - `README.md`（セットアップ）、`CONTRIBUTING.md`（貢献方法）
- **監視/ログ方針**
  - アプリ起動後に備え、ログ形式とメトリクス方針を統一
- **リリース戦略**
  - タグ運用、バージョニング、デプロイ責任範囲の明確化

## 4. 新規参加者向けの最初の1週間アクション例

1. Day 1: リポジトリ目的・プロダクト要件を確認
2. Day 2: 技術スタック候補を比較し提案
3. Day 3: 最小構成（Hello World + テスト1本 + CI）を作成
4. Day 4: コード規約/PRテンプレート整備
5. Day 5: README/CONTRIBUTINGを充実化

---

必要であれば次のステップとして、想定スタック（例: TypeScriptバックエンド）を前提に、
具体的な「初期ディレクトリ構成テンプレート」と「最初のPR計画」まで作成できます。
