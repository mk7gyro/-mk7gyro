{
  "name": "campaign-concept-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A full-stack campaign concept studio built with the OpenAI Responses API.",
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test",
    "check": "node --check app.js && node --check api/concept.js && node --check api/images.js && node --check src/campaign/config.js && node --check src/campaign/schemas.js && node --check src/campaign/prompts.js && node --check src/campaign/openai.js && node --check src/server/errors.js && node --test && node scripts/verify-sdk.mjs",
    "build": "mkdir -p public && cp index.html app.js styles.css public/ && npm run check"
  },
  "dependencies": {
    "openai": "6.48.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "vercel": "^56.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
