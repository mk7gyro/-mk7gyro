{
  "name": "launch-desk",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A full-stack engineering launch-planning agent built with the OpenAI Agents SDK.",
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test",
    "check": "node --check app.js && node --check api/agent.js && node --check api/health.js && node --check src/agent/config.js && node --check src/agent/schemas.js && node --check src/agent/tools.js && node --check src/agent/launch-agent.js && node --check src/agent/stream-events.js && node --check src/agent/observability.js && node --check src/server/ndjson.js && node --test && node scripts/verify-sdk.mjs",
    "build": "mkdir -p public && cp index.html app.js styles.css public/ && npm run check",
    "verify:e2e": "node scripts/verify-stream.mjs"
  },
  "dependencies": {
    "@openai/agents": "0.13.5",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "vercel": "^56.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
