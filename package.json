{
  "name": "launch-desk",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A full-stack engineering launch-planning agent built with the OpenAI Agents SDK.",
  "scripts": {
    "dev": "node src/server/local.js",
    "test": "node --test",
    "check": "node --check app.js && node --check api/agent.js && node --check api/health.js && node --check src/agent/config.js && node --check src/agent/launch-agent.js && node --check src/agent/tools.js && node --check src/agent/stream.js && node --check src/core/planning.js && node --check src/core/schemas.js && node --check src/server/http.js && node --check src/server/local.js && node --test && node scripts/verify-sdk.mjs",
    "build": "rm -rf public && mkdir -p public && cp index.html app.js styles.css public/ && npm run check",
    "verify:e2e": "node scripts/verify-stream.mjs"
  },
  "dependencies": {
    "@openai/agents": "0.13.5",
    "zod": "^4.0.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20 <23"
  }
}
